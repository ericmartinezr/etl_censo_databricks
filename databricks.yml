bundle:
  name: censo-etl

include:
  - resources/*.yml 

targets:
  prod:
    workspace:
      host: https://adb-7405606207367826.6.azuredatabricks.net
      root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    mode: production